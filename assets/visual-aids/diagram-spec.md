<!-- Diagram spec — a brief for an on-brand hexagon-system diagram (you or a designer builds the art;
     this file just specifies it). Tenable visual rules: built on the hexagon grid (0/60/120°); one
     distinct idea, never decoration; ONE color per icon; approved combos only — Soft Black on white,
     Highlight Yellow on Soft Black, or Soft Black on Highlight Yellow; Work Sans type, sentence case,
     left-aligned; a single yellow highlight max; never yellow text on white or white on yellow;
     products and exposure-management concepts are shown via hexagon illustration, NEVER photography. -->

# Diagram spec — <asset name>

- **One idea it must convey:** <the single concept — e.g. how the asset fits a workflow>
- **Elements (each a hexagon node):** <node 1> · <node 2> · <node 3>
- **Flow / relationship:** <how the nodes connect — arrows/adjacency on the hex grid>
- **Color treatment:** <one approved combo; where the single yellow highlight lands, if any>
- **Labels:** <Work Sans, sentence case, left-aligned; the few words on each node>
- **Alt text:** <descriptive, specific — required>
